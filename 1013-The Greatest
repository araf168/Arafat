#include <iostream>
 
using namespace std;
 
int main() {
 
    int  a, b, c, d, MaiorAB;
    scanf("%d%d%d", &a, &b, &c);
    d =(a+b+abs(a-b))/2;
    MaiorAB=(c+d+abs(c-d))/2;
    printf("%d eh o maior\n", MaiorAB);
 
    return 0;
}
